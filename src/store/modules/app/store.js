import Vue from 'vue'



const app = {
    namespaced: true,
    state: {
        theme:'default-theme',
        // drawer state,
        // footer options,
        // header & toolbar options
        // date formats, number formats etc....
    }
}

export default app

